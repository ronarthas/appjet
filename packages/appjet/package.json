{
  "name": "appjet",
  "version": "0.1.0",
  "description": "Modern desktop apps with web technologies",
  "main": "./src/appjet/index.ts",
  "types": "./src/appjet/index.ts",
  "module": "./src/appjet/index.ts",
  "type": "module",
  "bin": {
    "create-appjet": "./bin/create.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "bun build src/appjet/index.ts --outdir dist --target bun",
    "build:app": "bun run --env-file=.env --env-file=.env.production scripts/build.ts",
    "dev": "bun --env-file=.env --env-file=.env.development run test/basic-app.ts",
    "prepublish": "bun run build:framework"
  },
  "keywords": [
    "desktop",
    "app",
    "webview",
    "bun",
    "vue",
    "electron-alternative"
  ],
  "license": "MIT",
  "dependencies": {
    "webview-bun": "^2.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
